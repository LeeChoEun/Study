// 1300 K번째 수, 이분탐색 (골드 1)
// 크기가 N*N인 이차원배열 A (A[i][j] = i * j)
// A를 1차원 배열인 B로 옮기면 B의 크기는 N*N
// B를 오름차순 정리했을 때 B[k]

#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin >> N;
    int K;
    cin >> K;
    
    vector<int> B(N*N);
    for(int i = 0; i < N; i++)
    {

    }

    int result = 0;


    cout << result << "\n";
    return 0;
}